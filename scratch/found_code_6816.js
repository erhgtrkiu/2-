"const fs = require('fs');\nconst logPath = 'C:\\\\\\\\Users\\\\\\\\m3615\\\\\\\\.gemini\\\\\\\\antigravity\\\\\\\\brain\\\\\\\\68589a5c-391d-4fb4-bd7d-f6ad216b12ae\\\\\\\\.system_generated\\\\\\\\logs\\\\\\\\transcript.jsonl';\n\nif (fs.existsSync(logPath)) {\n    const content = fs.readFileSync(logPath, 'utf-8');\n    const lines = content.split('\\n');\n    \n    for (let i = 0; i < lines.length; i++) {\n        const line = lines[i];\n        if (line.includes('generateProceduralTextures') && line.includes('THREE.CanvasTexture')) {\n            // Check if this line contains more texture creation functions, like stairs or warning lights\n            const count = (line.match(/CanvasTexture/g) || []).length;\n            if (count > 1) {\n                console.log(`Found candidate at line ${i} with ${count} CanvasTexture matches`);\n                const idx = line.indexOf('function generateProceduralTextures()');\n                if (idx !== -1) {\n                    console.log(line.substring(idx, idx + 4000));\n                    console.log('--- NEXT 4000 ---');\n                    console.log(line.substring(idx + 4000, idx + 8000));\n                    break;\n                }\n            }\n        }\n    }\n}\n"